이 디렉토리는 java02 프로젝트에서 사용하는 
외부 라이브러리 파일(.jar)을 두는 디렉토리이다.

이 디렉토리에 있는 JAR(java archive(묶음)) 파일을 사용하려면  
프로젝트의 classpath에 등록해야 한다.

방법:
1) 프로젝트 속성창 > Java Build Path > Libraries 화면으로 간다.
2) [Add JARs] 버튼 클릭하여 해당 JAR 파일을 등록한다.
 
